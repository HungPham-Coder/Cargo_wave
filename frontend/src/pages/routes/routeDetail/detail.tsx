import { Button } from "antd";
// import { endLocations, startLocations, timelineData } from "../../constants/enum";

export const RoutesDetailPage = () => {
  return (
    <>
      <Button
        type="primary"
        style={{
          padding: "10px",
          height: "auto",
          backgroundColor: "#ffffff",
          display: "inline-block",
        }}
      >
        <div
          style={{
            display: "flex",
            flexDirection: "column",
            alignItems: "flex-start",
          }}
        >
        </div>
      </Button>
    </>
  );
};

export default RoutesDetailPage;
